import ContentLayout from '@/layout/ContentLayout';

const Header = () => {
  return (
    <div className="h-[84px] bg-slate-500">
      <ContentLayout>
        <h1 className="">헤더영역 입니다.</h1>
      </ContentLayout>
    </div>
  );
};

export default Header;
